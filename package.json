{
  "name": "superinstance-monorepo",
  "version": "0.3.0",
  "private": true,
  "description": "Conservation-law governance for AI agent fleets",
  "type": "module",
  "workspaces": [
    "sdk",
    "integrations/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm test --workspaces",
    "clean": "find . -name 'dist' -type d -exec rm -rf {} + 2>/dev/null; find . -name 'node_modules' -type d -exec rm -rf {} + 2>/dev/null"
  },
  "keywords": ["mcp", "multi-agent", "fleet", "conservation", "governance", "ai-agent", "ternary", "pid"]
}
